apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    uid: influxdb-puda
    url: http://influxdb:8181
    access: proxy
    isDefault: true
    database: machines
    jsonData:
      version: SQL
      database: machines
      insecureGrpc: true
    secureJsonData:
      token: apiv3_puda
