apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    uid: influxdb-puda
    url: $INFLUXDB_URL
    access: proxy
    isDefault: true
    database: $INFLUXDB_DATABASE
    jsonData:
      version: SQL
      database: $INFLUXDB_DATABASE
      insecureGrpc: true
    secureJsonData:
      token: $INFLUXDB_TOKEN

  - name: InfluxDB Hermes
    type: influxdb
    uid: influxdb-hermes-first
    url: $INFLUXDB_URL
    access: proxy
    jsonData:
      version: SQL
      database: hermes-first
      insecureGrpc: true
    secureJsonData:
      token: $INFLUXDB_TOKEN
